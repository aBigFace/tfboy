---
description: 禁止用串行/互斥闸门挡掉并发（定时请求、多波重叠必须保留）
alwaysApply: true
---

# 禁止人为串行（no serial gates）

本仓库里涉及**定时轮询、抢购波次、多请求重叠**时，**不得**用「等上一段跑完再发下一段」的方式把并发压成串行。

## 禁止的做法（示例）

- 用 `waveInFlight` / `inFlight` / `busy` 等标志：**若上一段未完成则直接 return / skip**，导致定时器到点也不发请求。
- 单全局互斥锁：**同一资源上只允许一条 in-flight**，其它 tick 全部丢弃（除非产品明确要求且用户写明）。
- 为实现「去重」而**牺牲重叠请求**：应先改资源模型（例如代理池 `shift` 取线、每波独立线路），而不是禁止重叠。

## 应优先的做法

- **允许 interval / 多次 `void foo()` 重叠**；通过池化、每请求取独立槽位、失败剔除+补货等，避免多路共抢同一条资源。
- 若必须限制并发度，用**有界队列 / semaphor** 等明确可配置策略，并在规则或注释里说明理由；默认仍不得「上一段不结束就完全不请求」。

## 自检

改完自问：定时/抢购场景下，**上一请求尚未结束（如仍在等 25s 超时）时，下一 tick 是否仍会照常发起**？若不会，则违反本规则。
