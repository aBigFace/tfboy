<view class="page">
    <view style="background:#fff;width:100%;height:100%">
        <scroll-view class="custom-tabs" scrollY="true" style="width:100%;height:92%">
            <image mode="widthFix" src="{{b}}" style="margin-left:2%;width:96%" wx:if="{{a}}"></image>
        </scroll-view>
    </view>
    <view class="btn-group" wx:if="{{c}}">
        <button bindtap="{{d}}" class="default-btn">取消</button>
        <button bindtap="{{e}}" class="primary-btn" type="primary">阅读并同意</button>
    </view>
</view>
