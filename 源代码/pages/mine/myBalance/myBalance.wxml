<view class="page">
    <view class="my-balance">
        <view class="banner">
            <view class="balance-num">
                <view class="name">小葵花数量</view>
                <view class="num">{{a}}</view>
            </view>
            <view bindtap="{{b}}" class="description">获取和使用</view>
        </view>
        <uni-forms bind:__l="__l" class="mobileForm r" uI="a3c322d8-0" uP="{{w}}" uR="formRef" uS="{{['d']}}" wx:if="{{w}}">
            <uni-forms-item bind:__l="__l" uI="a3c322d8-1,a3c322d8-0" uP="{{e}}" uS="{{['d']}}" wx:if="{{e}}">
                <view class="item">
                    <input bindinput="{{d}}" class="ba-input" placeholder="请填写TA的会员ID、手机号或邮箱" placeholderStyle="color:#BBBBBB" value="{{c}}"></input>
                </view>
            </uni-forms-item>
            <uni-forms-item bind:__l="__l" uI="a3c322d8-2,a3c322d8-0" uP="{{h}}" uS="{{['d']}}" wx:if="{{h}}">
                <view class="item">
                    <input bindinput="{{g}}" class="ba-input" placeholder="请填写转赠数值" placeholderStyle="color:#BBBBBB" value="{{f}}"></input>
                </view>
            </uni-forms-item>
            <uni-forms-item bind:__l="__l" uI="a3c322d8-3,a3c322d8-0" uP="{{k}}" uS="{{['d']}}" wx:if="{{k}}">
                <view class="item">
                    <input bindinput="{{j}}" class="ba-input" placeholder="请填写绑定手机号或邮箱" placeholderStyle="color:#BBBBBB" value="{{i}}"></input>
                </view>
            </uni-forms-item>
            <uni-forms-item bind:__l="__l" uI="a3c322d8-4,a3c322d8-0" uP="{{q}}" uS="{{['d']}}" wx:if="{{q}}">
                <view class="item">
                    <view class="ba-input code-view">
                        <input bindinput="{{m}}" class="code" maxlength="6" placeholder="请填写验证码" placeholderStyle="color:#BBBBBB" value="{{l}}"></input>
                        <view class="btn">
                            <text bindtap="{{o}}" class="text-tip" wx:if="{{n}}"> 获取验证码 </text>
                            <view class="text-tip" wx:else>{{p}}s</view>
                        </view>
                    </view>
                </view>
            </uni-forms-item>
            <button bindtap="{{s}}" class="primary-btn" disabled="{{r}}" type="primary">确认转赠</button>
            <view bindtap="{{t}}" class="rule">
                <image src="/static/image/mine/rule.png"></image>
                <text>规则说明</text>
            </view>
        </uni-forms>
    </view>
    <confirm-modal bind:__l="__l" class="r" uI="a3c322d8-5" uR="confirmModalRef"></confirm-modal>
</view>
